# Requirements for train_trackformer.py, analyze_results.py, compare_runs.py,
# error_breakdown.py, visualize_attention.py, calibration_test.py, plot_style.py
#
# Install with:
#   pip install -r requirements.txt
# (On the Dias cluster, do this inside your activated venv, after `module
# load Python/3.9.6-GCCcore-11.2.0`.)

# Data access
colliderml>=0.1.0
polars>=0.20

# Core numerics / dataframes
numpy>=1.24
pandas>=2.0
scipy>=1.10          # bootstrap CI helper + scipy.stats.wilcoxon, scipy.stats.norm

# ML
torch>=2.1
scikit-learn>=1.3    # StandardScaler
joblib>=1.3           # scaler serialization

# Plotting
matplotlib>=3.7
